<div class="comments">
  {{#each this}}
  <section class="comment">
<div class="meta">
  {{format_plural "posted" vote_count}} by {{user.username}} on {{format_date created_at}}
  |
  <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
</div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>